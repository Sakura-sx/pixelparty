{
	"name": "pixelparty",
	"private": true,
	"version": "1.0.0",
	"type": "module",
	"main": "server/index.js",
	"scripts": {
		"build": "cd client && npx pnpm@10 install && npx pnpm@10 run build",
		"start": "node server/index.js",
		"dev:server": "node server/index.js",
		"dev:client": "cd client && pnpm dev"
	},
	"dependencies": {
		"express": "^5.1.0",
		"socket.io": "^4.8.1"
	}
}
